# Configuration

The real local configuration files are intentionally excluded from this public repository.

1. Copy `database.example.php` to `database.php` and set your local database credentials.
2. Copy `admin_settings.example.php` to `admin_settings.php` and set a private administrator registration key.
3. Never commit those two local files to a public repository.
