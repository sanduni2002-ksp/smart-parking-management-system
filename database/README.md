# Database

Export the project database from phpMyAdmin and place the sanitized SQL schema/sample-data file in this folder.

Before publishing the SQL file, remove any real user accounts, personal information, password hashes, emails, phone numbers, or other sensitive data.
